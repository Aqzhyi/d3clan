// Generated by CoffeeScript 1.3.3
(function() {

  jQuery(function() {
    var app;
    app = {
      origin: {},
      entity: {}
    };
    app.origin.base = Backbone.View.extend({
      el: 'body',
      initialize: function() {
        app.entity.news_class = new app.origin.news_class;
        return app.entity.circle_news = new app.origin.circle_news;
      }
    });
    app.origin.news_class = Backbone.View.extend({
      el: jQuery('#bb-news_class'),
      initialize: function() {},
      events: {
        'click #menu_btn': 'switch_news_class'
      },
      switch_news_class: function(e) {
        var index;
        jQuery(e.currentTarget).addClass('current').siblings().removeClass('current');
        index = jQuery(e.currentTarget).index();
        return this.$el.find('[id=list_block]').eq(index).addClass('current').siblings().removeClass('current');
      }
    });
    app.origin.circle_news = Backbone.View.extend({
      el: jQuery('#bb-circle_news'),
      initialize: function() {},
      events: {
        'click #menu_btn': 'switch_circle_news'
      },
      switch_circle_news: function(e) {
        var index;
        jQuery(e.currentTarget).addClass('current').siblings().removeClass('current');
        index = jQuery(e.currentTarget).index();
        return this.$el.find('[id=list_block]').eq(index).addClass('current').siblings().removeClass('current');
      }
    });
    return app.entity.base = new app.origin.base;
  });

}).call(this);
